import 'package:flutter_test/flutter_test.dart';

void main() {
  // ponytail: placeholder — add integration tests when sqflite supports in-memory testing
  test('placeholder', () => expect(true, isTrue));
}
